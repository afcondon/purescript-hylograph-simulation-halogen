// Generated by purs version 0.15.15
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Show from "../Data.Show/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var tau = /* #__PURE__ */ (function () {
    return 2.0 * Data_Number.pi;
})();
var pieSlicePath = function (x0) {
    return function (y0) {
        return function (x1) {
            return function (y1) {
                return function (r) {
                    return function (largeArc) {
                        return function (sweep) {
                            return function (cx) {
                                return function (cy) {
                                    return "M" + (show(x0) + ("," + (show(y0) + ("A" + (show(r) + ("," + (show(r) + (" 0 " + (largeArc + ("," + (sweep + (" " + (show(x1) + ("," + (show(y1) + ("L" + (show(cx) + ("," + (show(cy) + "Z")))))))))))))))))));
                                };
                            };
                        };
                    };
                };
            };
        };
    };
};
var fullCirclePath = function (cx) {
    return function (cy) {
        return function (innerR) {
            return function (outerR) {
                var outerTop = show(cx) + ("," + show(cy - outerR));
                var outerBottom = show(cx) + ("," + show(cy + outerR));
                var innerTop = show(cx) + ("," + show(cy - innerR));
                var innerBottom = show(cx) + ("," + show(cy + innerR));
                var $9 = innerR < 1.0e-4;
                if ($9) {
                    return "M" + (outerTop + ("A" + (show(outerR) + ("," + (show(outerR) + (" 0 1,1 " + (outerBottom + ("A" + (show(outerR) + ("," + (show(outerR) + (" 0 1,1 " + (outerTop + "Z")))))))))))));
                };
                return "M" + (outerTop + ("A" + (show(outerR) + ("," + (show(outerR) + (" 0 1,1 " + (outerBottom + ("A" + (show(outerR) + ("," + (show(outerR) + (" 0 1,1 " + (outerTop + ("M" + (innerTop + ("A" + (show(innerR) + ("," + (show(innerR) + (" 0 1,0 " + (innerBottom + ("A" + (show(innerR) + ("," + (show(innerR) + (" 0 1,0 " + (innerTop + "Z")))))))))))))))))))))))))));
            };
        };
    };
};
var donutSegmentPath = function (ox0) {
    return function (oy0) {
        return function (ox1) {
            return function (oy1) {
                return function (ix0) {
                    return function (iy0) {
                        return function (ix1) {
                            return function (iy1) {
                                return function (outerR) {
                                    return function (innerR) {
                                        return function (largeArc) {
                                            return function (sweep) {
                                                return "M" + (show(ox0) + ("," + (show(oy0) + ("A" + (show(outerR) + ("," + (show(outerR) + (" 0 " + (largeArc + ("," + (sweep + (" " + (show(ox1) + ("," + (show(oy1) + ("L" + (show(ix1) + ("," + (show(iy1) + ("A" + (show(innerR) + ("," + (show(innerR) + (" 0 " + (largeArc + (",0 " + (show(ix0) + ("," + (show(iy0) + "Z")))))))))))))))))))))))))))));
                                            };
                                        };
                                    };
                                };
                            };
                        };
                    };
                };
            };
        };
    };
};
var degreesToRadians = function (deg) {
    return (deg * Data_Number.pi) / 180.0;
};
var arcPathWithCenter = function (cx) {
    return function (cy) {
        return function (config) {
            return function (datum) {
                var toSvgAngle = function (angle) {
                    return angle - Data_Number.pi / 2.0;
                };
                var sa = toSvgAngle(datum.startAngle);
                var outerStartY = cy + config.outerRadius * Data_Number.sin(sa);
                var outerStartX = cx + config.outerRadius * Data_Number.cos(sa);
                var innerStartY = cy + config.innerRadius * Data_Number.sin(sa);
                var innerStartX = cx + config.innerRadius * Data_Number.cos(sa);
                var ea = toSvgAngle(datum.endAngle);
                var innerEndX = cx + config.innerRadius * Data_Number.cos(ea);
                var innerEndY = cy + config.innerRadius * Data_Number.sin(ea);
                var outerEndX = cx + config.outerRadius * Data_Number.cos(ea);
                var outerEndY = cy + config.outerRadius * Data_Number.sin(ea);
                var angleDiff = datum.endAngle - datum.startAngle;
                var isFullCircle = angleDiff >= tau - 1.0e-4;
                var largeArcFlag = (function () {
                    var $12 = angleDiff > Data_Number.pi;
                    if ($12) {
                        return "1";
                    };
                    return "0";
                })();
                if (isFullCircle) {
                    return fullCirclePath(cx)(cy)(config.innerRadius)(config.outerRadius);
                };
                var $14 = config.innerRadius < 1.0e-4;
                if ($14) {
                    return pieSlicePath(outerStartX)(outerStartY)(outerEndX)(outerEndY)(config.outerRadius)(largeArcFlag)("1")(cx)(cy);
                };
                return donutSegmentPath(outerStartX)(outerStartY)(outerEndX)(outerEndY)(innerStartX)(innerStartY)(innerEndX)(innerEndY)(config.outerRadius)(config.innerRadius)(largeArcFlag)("1");
            };
        };
    };
};
var arcPath = /* #__PURE__ */ arcPathWithCenter(0.0)(0.0);
export {
    arcPath,
    arcPathWithCenter,
    degreesToRadians,
    tau
};
//# sourceMappingURL=index.js.map
