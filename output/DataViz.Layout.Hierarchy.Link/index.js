// Generated by purs version 0.15.15
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Show from "../Data.Show/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var StepHorizontal = /* #__PURE__ */ (function () {
    function StepHorizontal() {

    };
    StepHorizontal.value = new StepHorizontal();
    return StepHorizontal;
})();
var StepVertical = /* #__PURE__ */ (function () {
    function StepVertical() {

    };
    StepVertical.value = new StepVertical();
    return StepVertical;
})();
var BezierVertical = /* #__PURE__ */ (function () {
    function BezierVertical() {

    };
    BezierVertical.value = new BezierVertical();
    return BezierVertical;
})();
var BezierHorizontal = /* #__PURE__ */ (function () {
    function BezierHorizontal() {

    };
    BezierHorizontal.value = new BezierHorizontal();
    return BezierHorizontal;
})();
var BezierRadial = /* #__PURE__ */ (function () {
    function BezierRadial() {

    };
    BezierRadial.value = new BezierRadial();
    return BezierRadial;
})();
var Diagonal = /* #__PURE__ */ (function () {
    function Diagonal() {

    };
    Diagonal.value = new Diagonal();
    return Diagonal;
})();
var showLinkStyle = {
    show: function (v) {
        if (v instanceof StepHorizontal) {
            return "Step H";
        };
        if (v instanceof StepVertical) {
            return "Step V";
        };
        if (v instanceof BezierVertical) {
            return "Bezier V";
        };
        if (v instanceof BezierHorizontal) {
            return "Bezier H";
        };
        if (v instanceof BezierRadial) {
            return "Radial";
        };
        if (v instanceof Diagonal) {
            return "Diagonal";
        };
        throw new Error("Failed pattern match at DataViz.Layout.Hierarchy.Link (line 28, column 1 - line 34, column 29): " + [ v.constructor.name ]);
    }
};
var linkStepVertical = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                var midY = (y1 + y2) / 2.0;
                return "M" + (show(x1) + ("," + (show(y1) + ("V" + (show(midY) + ("H" + (show(x2) + ("V" + show(y2)))))))));
            };
        };
    };
};
var linkStepHorizontal = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                var midX = (x1 + x2) / 2.0;
                return "M" + (show(x1) + ("," + (show(y1) + ("H" + (show(midX) + ("V" + (show(y2) + ("H" + show(x2)))))))));
            };
        };
    };
};
var linkDiagonal = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                return "M" + (show(x1) + ("," + (show(y1) + ("L" + (show(x2) + ("," + show(y2)))))));
            };
        };
    };
};
var linkBezierVertical = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                var midY = (y1 + y2) / 2.0;
                return "M" + (show(x1) + ("," + (show(y1) + ("C" + (show(x1) + ("," + (show(midY) + (" " + (show(x2) + ("," + (show(midY) + (" " + (show(x2) + ("," + show(y2)))))))))))))));
            };
        };
    };
};
var linkBezierRadialCartesian = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                var radius2 = Data_Number.sqrt(x2 * x2 + y2 * y2);
                var radius1 = Data_Number.sqrt(x1 * x1 + y1 * y1);
                var midRadius = (radius1 + radius2) / 2.0;
                var angle2 = Data_Number.atan2(y2)(x2);
                var cp2x = midRadius * Data_Number.cos(angle2);
                var cp2y = midRadius * Data_Number.sin(angle2);
                var angle1 = Data_Number.atan2(y1)(x1);
                var cp1x = midRadius * Data_Number.cos(angle1);
                var cp1y = midRadius * Data_Number.sin(angle1);
                return "M" + (show(x1) + ("," + (show(y1) + ("C" + (show(cp1x) + ("," + (show(cp1y) + (" " + (show(cp2x) + ("," + (show(cp2y) + (" " + (show(x2) + ("," + show(y2)))))))))))))));
            };
        };
    };
};
var linkBezierRadial = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                var cr1 = (y1 + y2) / 2.0;
                var angle2 = ((x2 - 90.0) * Data_Number.pi) / 180.0;
                var cx2 = cr1 * Data_Number.cos(angle2);
                var cy2 = cr1 * Data_Number.sin(angle2);
                var tx = y2 * Data_Number.cos(angle2);
                var ty = y2 * Data_Number.sin(angle2);
                var angle1 = ((x1 - 90.0) * Data_Number.pi) / 180.0;
                var cx1 = cr1 * Data_Number.cos(angle1);
                var cy1 = cr1 * Data_Number.sin(angle1);
                var sx = y1 * Data_Number.cos(angle1);
                var sy = y1 * Data_Number.sin(angle1);
                return "M" + (show(sx) + ("," + (show(sy) + ("C" + (show(cx1) + ("," + (show(cy1) + (" " + (show(cx2) + ("," + (show(cy2) + (" " + (show(tx) + ("," + show(ty)))))))))))))));
            };
        };
    };
};
var linkBezierHorizontal = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                var midX = (x1 + x2) / 2.0;
                return "M" + (show(x1) + ("," + (show(y1) + ("C" + (show(midX) + ("," + (show(y1) + (" " + (show(midX) + ("," + (show(y2) + (" " + (show(x2) + ("," + show(y2)))))))))))))));
            };
        };
    };
};
var linkGenerator = function (v) {
    if (v instanceof StepHorizontal) {
        return linkStepHorizontal;
    };
    if (v instanceof StepVertical) {
        return linkStepVertical;
    };
    if (v instanceof BezierVertical) {
        return linkBezierVertical;
    };
    if (v instanceof BezierHorizontal) {
        return linkBezierHorizontal;
    };
    if (v instanceof BezierRadial) {
        return linkBezierRadial;
    };
    if (v instanceof Diagonal) {
        return linkDiagonal;
    };
    throw new Error("Failed pattern match at DataViz.Layout.Hierarchy.Link (line 196, column 1 - line 196, column 79): " + [ v.constructor.name ]);
};
var eqLinkStyle = {
    eq: function (x) {
        return function (y) {
            if (x instanceof StepHorizontal && y instanceof StepHorizontal) {
                return true;
            };
            if (x instanceof StepVertical && y instanceof StepVertical) {
                return true;
            };
            if (x instanceof BezierVertical && y instanceof BezierVertical) {
                return true;
            };
            if (x instanceof BezierHorizontal && y instanceof BezierHorizontal) {
                return true;
            };
            if (x instanceof BezierRadial && y instanceof BezierRadial) {
                return true;
            };
            if (x instanceof Diagonal && y instanceof Diagonal) {
                return true;
            };
            return false;
        };
    }
};
export {
    StepHorizontal,
    StepVertical,
    BezierVertical,
    BezierHorizontal,
    BezierRadial,
    Diagonal,
    linkStepHorizontal,
    linkStepVertical,
    linkBezierVertical,
    linkBezierHorizontal,
    linkBezierRadial,
    linkBezierRadialCartesian,
    linkDiagonal,
    linkGenerator,
    eqLinkStyle,
    showLinkStyle
};
//# sourceMappingURL=index.js.map
